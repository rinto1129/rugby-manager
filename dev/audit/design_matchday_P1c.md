# P1c スタッフ: 試合レポート再構成＋代理入力＋ダッシュボード＋HIA
## goal
staff/index.html を「試合イベント(cal type:'match')起点」に組み替える。(1) V.matchview を cal match ∪ md日付 の一覧にし提出n/N・怪我/HIA/攣り人数・メンバー表ボタンを出す、(2) goMatchReport(evId) で提出状況（提出率・未提出チップ→代理入力）と要対応トリアージ順の選手行を出す、(3) goAddMatchDay(evId,pid) で md v2 の代理新規入力（怪我ありは approved:true/proxy:true/recordedBy、既存あれば編集へ誘導）、(4) showEditMatchStaff/doEditMatchStaff を v2 対応（minutes訂正含む・旧mdは旧フォーム維持）、(5) delMatchDayStaff を confirm 無し＋Undo トーストに、(6) ダッシュボードの死コード 1815-1819 削除＋2109-2119 を「試合日チェック未入力（当日〜3日・日付ごと・代理入力ボタン・レポートリンク）」に置換、(7) 新着怪我カードに【HIA】最上位＋approveInjury で hia なら chartUpdate で isConcussion=true（trainer 安全ゲート起動）、(8) help 修正・doDelPlayer で cal.squad からも除去。9/5(土)分をこの画面から遡って代理入力できる状態にする。
## dataModel
【前提＝P1a基盤（staff に既に存在する想定。無ければ P1a を先に完了）】cal 試合イベント追加フィールド {opp, ko, venue, squad:[{pid,num}], squadAt}／identical ヘルパー: matchEvents()→cal type:'match' 配列(date昇順)、matchEventByDate(date)→ev|null、squadOf(ev)→[{pid,num}]（squad無しは[]）、squadRole(ev,pid)→'start'(num<=15)|'reserve'(16-23)|null、mdOf(pid,ev)→md|null（evId一致、evId無し旧mdは date 一致で救済）、mdRoleLabel(m)→'スタート'|'リザーブ'|'出場なし'（v2コード/旧文字列の両対応。旧'出場なし（ベンチ）'等はそのまま返す）、mdLoad(m)→rpe*minutes|null（旧mdは null）、pendingMatchChecks(pid,todayS,N=3)、mdInjuryLive(m)→injId一致の i が存在し approved!==false（旧mdは pid×date×source:'match' 照合）、mdSleepStr(m)→v2 'sleepH h'／旧 'sleepTime→wakeTime'、mdFatigueStr(m)→v2 '前x/5→後y/5'／旧 '前x/6→後y/6'（値<1は'未入力'）。
【前提＝P1b基盤（player で定義→staff へコピーし identical）】フォーム部品: rate5HTML/setRate5/_rate5OnChange/RATE5_LABELS/SORE_EMO（player 2771-2805）、partChipsHTML/togglePartChip/clearPartChips/bindSorePartsToggle（player 2807-2833）、汎用リストチップ listChipsHTML(id,list)/toggleListChip(id,i,val)（hidden input にカンマ区切り。仮名・P1bの実名に合わせる）、定数 MD_CRAMP_WHEN=['前半','後半','試合後']、MD_CRAMP_PARTS=['ふくらはぎ','ハムストリングス','大腿四頭筋','足裏','その他']、MD_HIA_SYMPTOMS=['頭痛','めまい','吐き気','もやもや','記憶が飛んだ','その他']、RATE5_LABELS に fatigue:['元気','軽い','ふつう','重い','限界']・perf:['悪い','いまいち','ふつう','良い','最高'] を追加。
【本フェーズで追加するフィールド（追加のみ・移行なし）】md: proxy:true（代理入力）, recordedBy:getCurrentUserName(MY_ROLE)='スタッフ', editedBy:'スタッフ'（代理修正時・新規）, 代理入力の md も v:2/evId/inputAt を持つ／i（代理入力で怪我あり）: source:'match', approved:true, approvedAt, approvedBy:'スタッフ', approvedByRole:'staff', proxy:true, mdId:md.id, canPractice, hia:true（脳震盪疑い自動起票 or injType==='脳震盪'）／chart（HIA承認時）: isConcussion:true, injType:'脳震盪', injDetail.scene が空なら '公式戦'（chartUpdate 経由・他フィールド不変）／cal.squad: 選手削除時に該当 pid を除去。
【本フェーズの staff ローカルヘルパー（将来 coach P5/trainer で共有→identical 昇格候補）】injIsHia(inj)→!!inj.hia||inj.type==='脳震盪'（trainer 前倒し時は identical 登録）／mdIsHiaSuspect(m)→!!m.hiaImpact&&(m.hiaSymptoms||[]).length>0／mdIsV2(m)→m.v===2||m.rpe!=null||m.minutes!=null／mdsOfEvent(ev)→D.md のうち (ev.id!=null&&m.evId!=null)?idEq(m.evId,ev.id):(m.date===ev.date)／matchChecksMissing(ev)→squadOf(ev) の pid のうち D.p に在籍し mdOf(pid,ev) が無い選手配列（squad空なら[]）／matchEventKey(ev)→ev.id!=null?String(ev.id):ev.date（疑似イベントは date 文字列）／resolveMatchEvent(key)→matchEvents().find(idEq(id,key))||matchEventByDate(key)||{id:null,date:key,title:'',squad:[]}（旧md専用の疑似イベント）／matchEventTitle(ev)→(ev.opp?'vs '+ev.opp:ev.title||'試合')+(ev.ko?' '+ev.ko+' KO':'')／mdTriage(m)→{rank,tags:[]}: rank0 HIA疑い(mdIsHiaSuspect)／1 怪我＋来週参加不可(mdInjuryLive&&canPractice==='参加できない')／2 怪我(mdInjuryLive)／3 攣り(m.cramp)／4 高負荷(m.rpe>=8 || mdLoad(m)>= 同試合の負荷上位25%)／5 睡眠<6h(v2: sleepH<6)／6 perf<=2／9 なし。tags は該当ラベル配列（'HIA疑い','怪我(来週不可)','怪我','攣り','高負荷','睡眠不足','パフォ低'）／hiaChartApply(injId,onDone)→chartUpdate(injId,ch=>{ch.isConcussion=true;ch.injType='脳震盪';if(!ch.injDetail)ch.injDetail={};if(!ch.injDetail.scene)ch.injDetail.scene='公式戦';},onDone)／svSafeSeq（player 1015 を staff へ移植・identical [player,staff,trainer]。trainer 850 とは正規化後一致を diff で確認済み）。
【読み側フォールバック（必須）】旧md（role文字列/fatiguePre・Post 1-6/sleepTime・wakeTime/evId無し）: 表示は mdRoleLabel/mdFatigueStr/mdSleepStr 経由、負荷列は '-'、レポート紐付けは date 一致、編集は旧フォーム分岐（mdIsV2 false）。旧 matchsel: 一切読まない（1817/2114/8909/8944 の参照は本フェーズで消える。8909/8944 は P1a で squad 化済みの想定）。evId 無しの ms/gs は本フェーズで触らない（P3）。squad 未登録の試合: 母数 N=0 → '提出 n名（メンバー表未登録）' 表示・未提出チップ無し・ダッシュボードには「メンバー表未登録」行を出す。i.hia 無しの旧脳震盪は injIsHia の type 判定で拾う。
## steps

### 1. [staff] 共通土台の移植: svSafeSeq＋P1b フォーム部品＋定数（identical 登録）
WHERE: staff/index.html: svSafeSeq は releaseSubmit(1255-1262) の直後・svSafe(1263) の前に挿入（player の isFilled→svSafeSeq→guardSubmit 順に合わせる）。rate5HTML 群・partChipsHTML 群・listChipsHTML/toggleListChip・MD_CRAMP_WHEN/MD_CRAMP_PARTS/MD_HIA_SYMPTOMS・RATE5_LABELS/SORE_EMO は numStepHTML(1623-1634) の直後にまとめて置く（ic() 定義より後・V 定義 1774 より前）。定数 MD_* は INJ_TYPES(987) 直後でも可。
CHANGE: player/index.html 1015-1038 の svSafeSeq をコメント込みでそのままコピー（本体は trainer 850 と正規化後一致）。P1b が player に作った rate5HTML/setRate5/_rate5OnChange/RATE5_LABELS/SORE_EMO/partChipsHTML/togglePartChip/clearPartChips/bindSorePartsToggle/listChipsHTML/toggleListChip/MD_* を一字一句コピー（staff には i-pain-1〜5・i-face-1〜5 の symbol が既にあるので SORE_EMO の ic() はそのまま動く）。既に P1a/P1b で staff に移植済みなら本ステップはスキップし manifest の files に staff が含まれているかだけ確認。
REUSE: player/index.html:1015 svSafeSeq / 2771-2833 rate5 系 / staff/index.html:1623 numStepHTML（既存・minutes/rpe/painに流用）
RISK: staff の var 定義順: SORE_EMO は ic() を呼ぶので ic 定義（staff 内で toast 447 より前に定義済み）より後に置くこと。sync_check の kind:'var' は `var NAME=...;` 1文で抽出されるため、定数は1行1宣言で書く。

### 2. [staff] 試合レポート用ローカルヘルパー群を追加
WHERE: staff/index.html: injLabel(1649) の直後・playerSearchHTML(1652) の前に新規ブロック『// ===== P1c 試合レポート/代理入力ヘルパー =====』を挿入。
CHANGE: dataModel に列挙した injIsHia / mdIsHiaSuspect / mdIsV2 / mdsOfEvent / matchChecksMissing / matchEventKey / resolveMatchEvent / matchEventTitle / mdTriage / hiaChartApply を function 宣言で追加。mdTriage の『負荷上位25%』は呼び出し側から同試合の mds を渡す第2引数 mdTriage(m, mds) とし、mds 省略時は rpe>=8 のみで判定（閾値計算: mds の mdLoad 非nullを降順ソートし Math.ceil(len*0.25) 位の値以上）。resolveMatchEvent は id 一致→date 一致→疑似イベントの順。全 id 比較は idEq。
REUSE: staff/index.html:1621 idEq / 2875 chartUpdate / 2849 _chartIdx（chart 無しでも雛形生成） / P1a: matchEvents・matchEventByDate・squadOf・mdOf・mdInjuryLive・mdLoad
RISK: P1a の関数名・戻り値が本設計の想定と違う場合はここで吸収する（薄いラッパにする）。同日2試合で evId 無しの旧 md は両イベントに紐づく（openIssues）。

### 3. [staff] V.matchview を試合イベント起点に再構成
WHERE: staff/index.html:2396-2418 V.matchview（tab0 の 2397-2414 を置換。tab1 週次怪我チェック 2417 と mvTab 5451 は不変）
CHANGE: 行ソース = matchEvents() ∪ 『cal に無い md 日付』（後者は {id:null,date,title:'（カレンダー未登録）',squad:[]} の疑似イベント）。todayS=todayStr() で分割: (A) UPCOMING = date>todayS の試合を昇順で上に小さく（kicker『UPCOMING』・タイトル matchEventTitle・『メンバー表 n名』バッジ・ボタン『メンバー表』→goSelectMatchMembers(ev.id,ev.date)（P1a の squad エディタ・既存名を維持）。カードクリック無し）。(B) REPORTS = date<=todayS を降順で。各カード: fmt(date)＋matchEventTitle(ev)、squad=squadOf(ev)、mds=mdsOfEvent(ev)、『提出 '+mds.length+(squad.length?'/'+squad.length+'名':'名（メンバー表未登録）')』、バッジ: 怪我 n（mds.filter(mdInjuryLive)）を bd-r、HIA n（mdIsHiaSuspect）を bd-r で『HIA n』（怪我より先頭）、足攣り n を bd-a、未提出 n（matchChecksMissing(ev).length、0なら非表示）を bd-a。提出0名でも squad があれば並ぶ。カード全体 onclick="goMatchReport('"+matchEventKey(ev)+"')"、右端に『メンバー表』ボタン（event.stopPropagation()・疑似イベントは非表示）。空なら『試合の予定・記録なし。カレンダーに試合を登録すると表示されます』。生hex禁止・var() のみ。
REUSE: staff/index.html:2406-2413 既存カード構造 / 2547-2557 の flag『MATCH』様式 / ic('i-ball') / 8907 goSelectMatchMembers（P1a改修後）
RISK: onSnapshot で再描画される主画面なので状態を DOM に持たない（現状と同じ）。旧 md 日付が cal と同日なら union で重複しないよう date でキー化する。

### 4. [staff] goMatchReport(evKey) 新設（goMatchDateDetail の置換・提出状況＋トリアージ）
WHERE: staff/index.html:5453-5475 goMatchDateDetail を『function goMatchDateDetail(date){goMatchReport(date);}』の薄い互換エイリアスに縮め、その直後に goMatchReport(evKey,replace) を新設。呼び出し元 2406/5163/5519 は goMatchReport に置換。
CHANGE: ev=resolveMatchEvent(evKey); key=matchEventKey(ev); squad=squadOf(ev); mds=mdsOfEvent(ev); missing=matchChecksMissing(ev)。HTML ルートを <div id="mr-root"> で包む（Undo/編集後の in-place 再描画判定に使う）。①ヘッダ: ic('i-ball')＋fmt(date)＋matchEventTitle(ev)＋(ev.venue)。②メトリクス grid g4: 提出 mds.length+(squad.length?'/'+squad.length:'')／怪我(mdInjuryLive, var(--red))／HIA疑い(mdIsHiaSuspect, var(--red))／足攣り(var(--amber))。③提出状況カード: kicker『MATCH CHECK-IN』、squad.length>0 なら pitchProgressHtml((mds.length/squad.length)*6,7,['G','22','10','HW','10','22','G'],{still:false,ball:ic('i-ball',20,'var(--pitch-accent)')}) と『未提出 (n名)』chip-late（onclick="goAddMatchDay('"+key+"',pid)"）、全員提出なら『全員提出済み』（submissionPanel 1994-1996 と同型）。squad が無ければ alert-info『メンバー表が未登録です → 登録する』（goSelectMatchMembers）。常設ボタン『+ 代理入力（メンバー外も可）』→goAddMatchDay(key,null)。④要対応: rows=mds.map(m=>({m,t:mdTriage(m,mds)})) を rank 昇順→inputAt 降順。rank<=6 を『要対応』見出し（ic('i-warn',15,'var(--red)')＋件数）、それ以外を『その他の提出』見出しに分ける。行カード: avH(p,32)＋名前＋roleBadgeHtml(mdRoleLabel(m))＋(m.num?'#'+m.num:'')＋2行目 v2: '出場 '+minutes+'分 / RPE '+rpe+' / 負荷 '+mdLoad(m)+' AU / 睡眠 '+mdSleepStr(m)、旧md: '疲労: '+mdFatigueStr(m)+' / 睡眠 '+mdSleepStr(m)。タグ chips: t.tags を HIA疑い・怪我(来週不可) は bd-r、怪我 bd-r、攣り bd-a、高負荷 bd-a、睡眠不足 bd-a、パフォ低 bd-p。canPractice==='参加できない' は bd-r『来週参加不可』。3行目: '提出 '+inputAt(HH:MM)+(m.proxy?' <span class="bd bd-b">代理</span>':'')+(m.editedAt?' 修正済':'')。ボタン: 修正→showEditMatchStaff(m.id,key)／削除→delMatchDayStaff(m.id,key)（event.stopPropagation）。カードクリック→goMatchDetail(m.id)。選手不在（削除済み）は名前『(削除済み選手)』で表示し握り潰さない。⑤末尾: 『この試合をCSV』は P5（本フェーズは出さない）。replace=true のとき pushView せず $m().innerHTML=h のみ（タイトル据え置き）。
REUSE: staff/index.html:1984-1999 submissionPanel（pitchProgressHtml＋chip-late） / 5462-5473 既存行カード / 411 roleBadgeHtml / avH / 447 toast
RISK: pitchProgressHtml の opts.still: サブ画面は毎回アニメ可（still:false）だが .rv-armed を文字列に書かない。負荷上位25% は提出が少ない試合でノイズになる→rpe>=8 を主条件、上位25% は mds.length>=8 のときのみ有効にする。

### 5. [staff] goMatchDetail を v2 項目・HIA・怪我リンク・提出メタ対応に拡張
WHERE: staff/index.html:8559-8579 goMatchDetail
CHANGE: find を idEq に統一（8560 の x.id===mid と p 検索）。基本情報: 選手／試合（fmt(date)＋matchEventTitle(resolveMatchEvent(m.evId!=null?m.evId:m.date))）／出場 roleBadgeHtml(mdRoleLabel(m))＋(#num)／v2: 出場時間 minutes分・RPE・試合負荷 mdLoad AU・睡眠 mdSleepStr・疲労 mdFatigueStr・筋肉痛 soreness/5＋(sorenessParts.join('・'))・パフォーマンス perf/5、旧: 疲労 mdFatigueStr・前日の睡眠 mdSleepStr／朝食＋escapeHtml(breakfastDetail)。HIA ボックス（mdIsHiaSuspect）: 見出し var(--red)『脳震盪スクリーニング【HIA】』、症状 hiaSymptoms.join('・')、hiaInjId があれば『怪我レコードを開く』→goInjuryDetail(hiaInjId)。怪我ボックス（m.injured）: 既存行＋『来週の練習』canPractice（'参加できない' は var(--red)）＋injId があれば『怪我管理で開く』→goInjuryDetail(m.injId)＋承認状態バッジ（D.i 該当の approved: null=承認待ち bd-a／true=承認済 bd-g／false=却下 bd-p）。injHow を escapeHtml。攣りボックス: crampWhen／crampParts.join('・')／crampFreq。メモ escapeHtml(m.note)。提出メタ: inputAt（日時）／proxy なら『代理入力（'+recordedBy+'）』／editedAt・editedBy。末尾に 修正/削除ボタン（showEditMatchStaff/delMatchDayStaff）。
REUSE: staff/index.html:8562-8577 既存 detail-row / 2898 goInjuryDetail / 1649 injLabel
RISK: 旧データで undefined が文字列化しないよう各値に (v!=null?v:'-') ガード。

### 6. [staff] playerSearchHTML に選択フックを追加（代理入力の背番号/ロール自動提案用）
WHERE: staff/index.html:1657 selectPSearch
CHANGE: 末尾に `if(window._pSearchOnSelect&&typeof window._pSearchOnSelect[id]==='function')window._pSearchOnSelect[id](pid);` を1行追加。既存呼び出し（nf-p/ni-p/nph-p 等）は _pSearchOnSelect 未設定なので無影響。goAddMatchDay 側で pushView 後に window._pSearchOnSelect['smd-p']=smdSyncSquad を登録し、popView 時に残っても次回上書きされるだけなので掃除不要。
REUSE: staff/index.html:1652-1658 playerSearchHTML 一式
RISK: なし（追加のみ）。

### 7. [staff] goAddMatchDay(evKey,pid) 代理新規入力フォーム
WHERE: staff/index.html: doEditMatchStaff(5502-5520) の直後（『// === TAPING RESERVATION ===』5522 の前）に goAddMatchDay / smdSyncSquad / smdToggle 群 / doAddMatchDay を新設。
CHANGE: ev=resolveMatchEvent(evKey)。pushView('試合日チェック 代理入力', html, fn)。html（max-width:560px）: 上部 flag『MATCH DAY』＋fmt(ev.date)＋matchEventTitle(ev)（日付は変更不可・hidden #smd-date=ev.date, #smd-ev=ev.id||''）。①選手 playerSearchHTML('smd-p',pid)＋pid あれば名前プリセット（goAddInjury 4435 と同型）。②出場: select #smd-role（value start/reserve/none・表示 スタート/リザーブ/出場なし）、#smd-num number 1-23、#smd-min number 0-120 placeholder『分』（role none なら 0 固定・disabled）、#smd-rpe numStepHTML 1-10、負荷プレビュー #smd-pv（RPE×分 AU・updSrpe 4473 と同型の smdUpdLoad）。③コンディション: #smd-sleeph number step .5、#smd-bf select はい/いいえ＋#smd-bftext、#smd-pre / #smd-post rate5HTML(SORE_EMO→疲労用は i-face 逆順=STRESS_EMO を使う・RATE5_LABELS.fatigue)、#smd-sore rate5HTML(SORE_EMO,RATE5_LABELS.sore)＋#smd-soreparts-wrap(partChipsHTML('smd-soreparts')・bindSorePartsToggle で soreness>=2 のとき表示)、#smd-perf rate5HTML(STRESS_EMO 逆順 or i-face, RATE5_LABELS.perf)。④怪我: #smd-inj select いいえ/はい→#smd-inj-detail（side/part/type/hist/how/painat/painnow/cont/canprac＝player 4333-4339 と同 id 体系で smd- 接頭）。⑤攣り: #smd-cramp いいえ/はい→#smd-cramp-detail（#smd-cwhen select MD_CRAMP_WHEN、listChipsHTML('smd-cparts',MD_CRAMP_PARTS)、#smd-crampfreq 既存3択）。⑥脳震盪: #smd-hia select『頭部への強い衝撃: なし/あり』→#smd-hia-detail（listChipsHTML('smd-hiasym',MD_HIA_SYMPTOMS)＋注記『衝撃あり＋症状1つ以上で脳震盪疑いの怪我を自動登録（承認済み扱い）』）。⑦メモ #smd-note。⑧ボタン: キャンセル popView／『記録する』onclick=doAddMatchDay('key',this)。fn: pid があれば smdSyncSquad(pid)。smdSyncSquad(pid): r=squadRole(ev,pid); #smd-role=r||'none'; #smd-num=squad の num||''; #smd-min: start→80, reserve→''（要入力）, none→0(disabled)。既に mdOf(pid,ev) があれば alert-card『この選手は入力済みです』＋『修正画面へ』ボタン（showEditMatchStaff）を #smd-dup に出し記録ボタンを disabled。ダッシュボード/レポートの未提出チップ・『+ 代理入力』から呼ぶ。
REUSE: staff/index.html:4467-4472 goAddFatigue（pushView＋playerSearchHTML 雛形） / 4424-4435 goAddInjury（select 群・プリセット） / 1623 numStepHTML / P1b 部品 rate5HTML・partChipsHTML・listChipsHTML
RISK: フォーム id は 'smd-' 接頭で既存 'md-'/'emds-' と衝突させない。rate5 の初期値は空＝必須チェックで拾う。pushView の fn は setTimeout 80ms（テストでは __timeouts を手動実行）。

### 8. [staff] doAddMatchDay(evKey,btnEl) 保存（必須ガード→重複ガード→svSafeSeq→HIA/怪我連鎖・冪等再送）
WHERE: staff/index.html: ステップ7の直後
CHANGE: 検証は必ず guardSubmit の前（順序: pid→role→minutes→rpe→sleepH→pre/post→怪我→攣り→重複）。pid=parseInt(#smd-p-val)|| alert('選手を選択してください')。role=#smd-role。minutes: role==='none'→0、それ以外 isFilled かつ 0〜120 でなければ alert('出場時間（分）を入力してください（0〜120）')。rpe: minutes>0 なら 1〜10 必須 alert('試合のきつさ RPE（1〜10）を入力してください')、minutes===0 なら任意（未入力は null）。sleepH: isFilled かつ 0〜14 alert('前日の睡眠時間（h）を入力してください')。pre/post: 1〜5 alert('試合前・試合後の疲労度（1〜5）を選択してください')。injured=='はい' なら painAt/painNow 1〜10 alert。cramp=='はい' なら crampWhen 必須。hiaImpact=(#smd-hia==='あり'), hiaSymptoms=csv→配列。重複: dup=mdOf(pid,ev) があれば toast('この選手は入力済みです。修正画面を開きます'); showEditMatchStaff(dup.id,evKey); return。→ guardSubmit(btnEl,'保存中…')。冪等再送: sig=pid+'|'+ev.date; window._smdPending が同 sig なら items=_smdPending.items.slice(_smdPending.done.length)（id 再採番しない）、違えば新規組立。新規組立: now=ISO, who=getCurrentUserName(MY_ROLE); rec={id:newId(),pid,date:ev.date,evId:ev.id!=null?ev.id:null,inputAt:now,v:2,role,num:num||null,minutes,rpe:rpe||null,sleepH,breakfast,breakfastDetail,preFatigue,postFatigue,soreness:sore||null,sorenessParts,perf:perf||null,injured,cramp,hiaImpact,hiaSymptoms,note,proxy:true,recordedBy:who}。injured なら injRec={id:newId(),pid,side,part,partOther:'',type,typeOther:'',date:ev.date,returnDate:'',hospitalVisited:false,hospitalDate:'',note:'【試合で受傷・代理入力】'+how+' / 痛み:受傷時'+painAt+' 現在'+painNow+' / '+hist+' / プレー続行:'+cont+' / 来週練習:'+canPrac,resolved:false,source:'match',canPractice,approved:true,approvedAt:now,approvedBy:who,approvedByRole:MY_ROLE,proxy:true,mdId:rec.id}（type==='脳震盪' なら injRec.hia=true）、rehabRec={id:newId(),injId:injRec.id,pid,stage:0,logs:[],stageDates:[]}、rec.injId=injRec.id、rec.inj*（injSide/injPart/injType/injHistory/injHow/injPainAt/injPainNow/injContinued/canPractice）。HIA: hiaImpact&&hiaSymptoms.length&&!(injured&&type==='脳震盪') なら hiaRec={...injRec 同型, side:'', part:'頭部', type:'脳震盪', note:'【HIA・試合で頭部衝撃】症状: '+hiaSymptoms.join('・')+(note?' / '+note:''), hia:true, source:'match', approved:true, approvedBy..., proxy:true, mdId:rec.id, canPractice:'わからない'}＋hiaRehab、rec.hiaInjId=hiaRec.id（injured&&type==='脳震盪' の場合は rec.hiaInjId=injRec.id）。cramp なら rec.crampWhen/crampParts/crampFreq。items=[i(inj)?,r?,i(hia)?,r(hia)?,md] の順。svSafeSeq(items, onAll: _smdPending=null; hiaIds=[rec.hiaInjId].filter(Boolean) を hiaChartApply で順に適用（chartUpdate 連鎖）→ popView(); goMatchReport(evKey); toast('代理入力しました'+(injured||rec.hiaInjId?'（怪我は承認済みで登録）':'')), onError(err,done,item): _smdPending={sig,items,done}; releaseSubmit(btnEl); alert('保存できませんでした（'+done.length+'/'+items.length+'件保存済み）。もう一度「記録する」を押すと未保存分だけ再送します'))。
REUSE: staff/index.html:4474 doAddFatigue（parse/alert様式） / 4437-4446 doAddInjury（staff起票の i/r 形・approvedBy）/ 1240 newId / 1242 isFilled / 1244 guardSubmit / 4093 getCurrentUserName / P1a mdOf / ステップ1 svSafeSeq / ステップ2 hiaChartApply
RISK: svSafeSeq は全か無ではない→_smdPending で done 以降のみ再送（player P1b の _mdPending と同じ発想）。重複ガードは事前チェックのみ（svSafeSeq 内で再判定できない）→選手の自己提出と同時刻に競合しうる（openIssues）。HIA の chart 作成は md 保存後の追加トランザクション（失敗しても i は approved 済みなので approveInjury 経路では再適用されない→toast で『カルテの脳震盪フラグ設定に失敗。怪我管理→評価タブで「脳震盪として扱う」を ON にしてください』と案内）。

### 9. [staff] showEditMatchStaff / doEditMatchStaff を v2 対応（旧mdは旧フォーム維持）＋minutes 訂正
WHERE: staff/index.html:5478-5501 showEditMatchStaff(mid,evKey) / 5502-5520 doEditMatchStaff(mid,evKey,btnEl)（第2引数を date→evKey に意味変更。呼び出し元はステップ4/5）
CHANGE: m=D.md.find(idEq)。mdIsV2(m) で分岐。【v2】フォーム: #emds-ev select（matchEvents() 降順・label fmt(date)+' '+matchEventTitle・value=id、現在 evId を selected。旧 evId 無し v2 は date 一致で選択）／#emds-role(start/reserve/none)／#emds-num／#emds-min／#emds-rpe numStepHTML／#emds-sleeph／#emds-bf・#emds-bftext／#emds-pre・#emds-post・#emds-sore(+#emds-soreparts)・#emds-perf は rate5HTML／攣り #emds-cramp(はい/いいえ 常時表示)→#emds-cwhen/#emds-cparts/#emds-crampfreq／#emds-note。怪我・HIA は編集不可: alert-info『怪我・脳震盪疑いの修正・取消は「怪我管理」から』（injId/hiaInjId があれば goInjuryDetail ボタン）。fn（80ms後）: 各値を代入、rate5 は setRate5(id,v) で初期化、chips は toggleListChip/togglePartChip で初期化、cramp が いいえ なら detail 非表示。【旧md】現行 5482-5487 のフォームをそのまま維持し #emds-date（input date）だけ追加（誤日付の自己修復・確定バグ#1）。doEditMatchStaff: v2 は検証（minutes 0-120／minutes>0 なら rpe 1-10／sleepH 0-14／pre・post 1-5）を guardSubmit の前に置き、svSafeUpdate('md') の updateFn 内で idEq 再探索→ role/num/minutes/rpe/sleepH/breakfast/breakfastDetail/preFatigue/postFatigue/soreness(0なら delete＋sorenessParts delete)/perf(0なら delete)/note を上書き、evId 変更時は m.evId と m.date を選択イベントに合わせる、cramp: はい→cramp=true＋crampWhen/crampParts/crampFreq、いいえ→cramp=false＋3項目 delete。旧md は既存ロジック＋ m.date=#emds-date（入力があれば）。共通: m.editedAt=ISO, m.editedBy=getCurrentUserName(MY_ROLE)。inputAt/proxy/recordedBy/injured/inj*/hia* は不変。成功: popView(); goMatchReport(evKey); toast('修正しました')。失敗: releaseSubmit＋alert。
REUSE: staff/index.html:5478-5520 既存フォーム・保存（旧分岐として温存）/ 4493-4495 doEditFatigueStaff の『検証→guardSubmit』順 / ステップ1 rate5HTML・partChipsHTML・listChipsHTML
RISK: evId 変更で別試合へ移すと mdsOfEvent の集計が移る＝意図通り。ただし旧 md の date 変更は cal 一致を壊しうるので、保存後の遷移先は変更後 date の resolveMatchEvent に合わせる。

### 10. [staff] delMatchDayStaff を confirm 無し・即削除＋Undo トーストに
WHERE: staff/index.html:5155-5164 delMatchDayStaff(mid,evKey)
CHANGE: 5156 の confirm を削除。snap=D.md.find(idEq(x.id,mid)) を退避（無ければ return）。svSafeUpdate('md', latest=>latest.filter(!idEq(r.id,mid)) ・before/after で notFound) → 成功: popView(); goMatchReport(evKey); toast('削除しました'+(snap.injured||snap.hiaInjId?'（怪我報告は残ります）':''),'元に戻す',function(){svSafeUpdate('md',function(latest){if(!latest.some(function(x){return idEq(x.id,snap.id);}))latest.push(snap);return latest;},function(){if(document.getElementById('mr-root'))goMatchReport(evKey,true);else if(V[curPage])V[curPage]();toast('元に戻しました');},function(){alert('復元に失敗しました。もう一度お試しください');});})。onError: alert('削除できませんでした')。i/r は触らない（D9）。
REUSE: staff/index.html:8602-8635 doRejectInjury/undoRejectInjury（snapshot＋Undo の手本）/ 447 toast(msg,actionLabel,fn)
RISK: Undo 中に他端末が同 id を再追加していれば重複しない（some ガード）。5秒以内の Undo のみ（toast 仕様 5200ms）。

### 11. [staff] ダッシュボード: 死コード削除＋『試合日チェック未入力（当日〜3日）』パネル
WHERE: staff/index.html:1815-1819（削除）／1984-1999 submissionPanel の直後（2000 付近）に matchPanel 生成を追加／2101 `submissionPanel+` の直後に `matchPanel+` を差し込み／2109-2119 の旧『昨日の試合日チェック未入力』ブロックを削除
CHANGE: 1815-1819（コメント行含む）を丸ごと削除（matchNotDone 参照は他に無い）。matchPanel: N=3; from=toDateStr(today-3日); evs=matchEvents().filter(e=>e.date>=from&&e.date<=todayS) を日付降順。evs が空なら ''。各 ev: squad=squadOf(ev), mds=mdsOfEvent(ev), missing=matchChecksMissing(ev)。カード class="card rv" style="border-left:4px solid var(--maroon)": kicker『MATCH CHECK-IN』、見出し ic('i-ball',15,'var(--maroon)')＋'試合日チェック '+fmt(ev.date)+' '+matchEventTitle(ev)＋右に『提出 n/N』（squad 無しは『メンバー表未登録』bd-a＋『登録する』→goSelectMatchMembers(ev.id,ev.date)）。squad あり: pitchProgressHtml((mds.length/squad.length)*6,7,[...],{still:true,ball:...})、missing.length なら『未入力 (n名)』＋chip-late（onclick="goAddMatchDay('"+key+"',"+p.id+")"）、0 なら『全員提出済み』（var(--green)）。追加で HIA疑い n / 怪我 n を bd-r で見出し右に。フッター: 『レポートを開く』→goMatchReport(key)。同 pid で md が『当日以降に提出されるまで催促・最大3日』= 窓内の試合ごとに独立表示（D4）。当日の試合も対象（試合当日の夜まで）。
REUSE: staff/index.html:1984-1999 submissionPanel（kicker/pitch/chip-late 様式）/ 2118 旧ブロックの見出し文言 / P1a matchEvents・squadOf / ステップ2 matchChecksMissing・mdsOfEvent
RISK: innerHTML+= 方式（2108/2118）から文字列連結方式に変えるので位置が『提出状況パネルの直後』に移る（意図的）。still:true 必須（メインタブ・onSnapshot 再描画）。squad 空の試合は催促対象外（D9）だがカードは出す。test_dash_staff は D.cal=[] なので影響なし。

### 12. [staff] 新着怪我カードに【HIA】最上位＋approveInjury の svSafeUpdate 化＋脳震盪カルテ連携
WHERE: staff/index.html:1807 pendingInj のソート／1910-1911 新着怪我カード／2193 V.injury の injCard（任意）／8581-8590 approveInjury
CHANGE: 1807: sort を (b,a)=> (injIsHia(b)?1:0)-(injIsHia(a)?1:0) || b.date.localeCompare(a.date) に（HIA 最上位）。1911: 行頭に injIsHia(x)?'<span class="bd bd-r" style="font-weight:800">【HIA】脳震盪疑い</span> ':'' を追加、source==='match' の『【試合】』は維持、x.proxy なら『代理』bd-b、hia なら承認ボタン文言を『怪我人に追加（脳震盪として）』に、行全体を alert-down のまま border-left を var(--red) 4px で強調。approveInjury(iid): svRec を廃止し svSafeUpdate('i', latest=>{idx=findIndex(idEq); if(idx<0){notFound=true;return latest;} if(latest[idx].approved===false){blocked=true;return latest;} 対象に approved=true, approvedAt=ISO, approvedBy=getCurrentUserName(MY_ROLE), approvedByRole=MY_ROLE, notifyPlayer=true を in-place 設定; isHia=injIsHia(latest[idx]); return latest;}, onDone: notFound→alert('報告が見つかりませんでした')／blocked→alert('この報告は既に却下されています')／isHia→hiaChartApply(iid,function(){V.dash();toast('脳震盪として承認しました。トレーナー画面の安全ゲートが有効になります');})／それ以外→V.dash();toast('怪我人に追加しました'), onError: alert('保存できませんでした。もう一度お試しください'))。2193 injCard: injIsHia(i) なら『HIA』bd-r チップを追加（任意・小）。
REUSE: staff/index.html:8602-8622 doRejectInjury（updateFn 内再判定・blocked の型）/ 2875 chartUpdate / 3476 saveChartMetrics の isConcussion 設定例 / trainer 1387 trTodoBadge（isConcussion で rank0 安全ゲート＝受け側は無改修）
RISK: dev/test_p7c.js 73-82 は approveInjury 後 drain() で store('i') を検証＝svSafeUpdate 化でも approvedBy/approvedByRole/approvedAt/notifyPlayer が入るので維持される（source:'player' 非HIA なので chart 連鎖なし）。HIA では onDone 内でさらに Promise が走るためテストは drain() を2回。

### 13. [staff] help 文言修正＋doDelPlayer で cal.squad から除去＋CSV の最小互換
WHERE: staff/index.html:2366-2368 help『試合日関連』／5045-5070 doDelPlayer（5067 の 'a' の後に1段追加）／5376 exportCSV('matchday')
CHANGE: help: 3行を『<b>出場メンバー登録：</b>「カレンダー」→ 試合の「出場選手設定」／「試合日レポート」→ 試合の「メンバー表」<br><b>試合日チェックの確認：</b>「試合日レポート」→ 試合を選択（提出状況・要対応順・未提出者はその場で代理入力）<br><b>未提出の代理入力：</b>ダッシュボード「試合日チェック未入力」の名前をタップ<br><b>脳震盪疑い（HIA）：</b>新着怪我に【HIA】が付く。「怪我人に追加」で脳震盪カルテになり、トレーナーの安全ゲートが動く<br>』に置換。doDelPlayer: `.then(function(){return svSafeUpdate('cal',function(latest){(latest||[]).forEach(function(e){if(e&&Array.isArray(e.squad))e.squad=e.squad.filter(function(s){return!idEq(s.pid,pid);});});return latest||[];});})` を 'a' の直後に追加（旧 matchsel は D2 により触らない）。5046 の confirm は本フェーズでは残置（選手削除は本フェーズ対象外・openIssues）。exportCSV('matchday'): 列は変えず、値だけ mdRoleLabel(m)（escapeHtml を外す）／疲労は mdFatigueStr(m) を前後に分割せず1列『疲労』としてまとめる代わりに既存2列は v2: preFatigue/postFatigue、旧: fatiguePre/fatiguePost を (v!=null?v:'') で出力／睡眠 mdSleepStr(m)／怪我は mdInjuryLive(m)?'あり':'なし'。全面刷新は P5。
REUSE: staff/index.html:5067 'a' のネスト更新パターン / P1a mdRoleLabel・mdSleepStr・mdInjuryLive
RISK: help は文字列のみ。cal 更新は squad 未定義イベントを触らない。

### 14. [trainer] 【任意・親プランの前倒し】trainer に md 購読＋T.home『試合後の要ケア』カード
WHERE: trainer/index.html:214 SK に md:'rm_matchday' を追加／216 D に md:[] を追加（cal は購読済み）／1438 ppCardHtml の直後（1440『今日のテーピング』の前）にカード挿入／ヘルパー injIsHia・mdIsHiaSuspect・mdsOfEvent・mdTriage・matchEventTitle を trainer にコピー（P1a の matchEvents/squadOf/mdOf/mdInjuryLive/mdLoad/mdRoleLabel も trainer に必要→P1a の files 拡張）
CHANGE: evs=matchEvents().filter(date>=today-3&&date<=today) 降順で最新1件。mds=mdsOfEvent(ev)。rows=mds.map(m=>({m,t:mdTriage(m,mds)})).filter(rank<=3)（HIA→怪我(来週不可)→怪我→攣り）を rank 順。加えて D.i の source:'match'&&date===ev.date で md に無い怪我も行に足す。カード（ダーク・var() のみ・border-left var(--red)）: 見出し『試合後の要ケア '+fmt(ev.date)+' '+matchEventTitle(ev)、行: avH(p,26)＋名前＋タグ（HIA疑い/怪我/攣り＋部位）＋承認状態: 該当 i の approved===null なら bd-a『承認待ち』（クリック不可・リハ対象に入れない）、approved===true なら『カルテへ』→goRehabPlayer(injId)。rows 空なら『要ケアの報告はありません』。SK 追加により startListeners(4136) が自動購読。
REUSE: trainer/index.html:1465 T.home の承認フィルタ（approved===true のみリンク）/ 1499-1508 リハビリ行カード様式 / 1387 trTodoBadge
RISK: trainer は md を初めて購読＝データ量増（md は tlog より遥かに小さい）。P5 で本実装するなら本ステップは省略可（openIssues）。sync_manifest の files に trainer を足すこと。

### 15. [dev] sync_manifest 登録＋sync_check/residue＋run_tests 全緑の確認
WHERE: dev/sync_manifest.json identical セクション／python3 dev/sync_check.py／--residue／python3 dev/run_tests.py
CHANGE: syncManifest 欄の通り登録。chart_counts は不変（new Chart 追加なし）。residue: 新規マークアップは var() のみ。run_tests: 既存 test_dash_staff / test_p7c / test_mstat / test_staff_ia_p8d / test_dash / test_home_p8b が緑のまま、新規3本（＋trainer任意1本）追加。
REUSE: dev/sync_check.py / dev/run_tests.py
RISK: kind:'var' 抽出は `var NAME=...;` 単文のみ→定数は1行で書く。

## tests
- 【新規】dev/test_matchday_report_staff.js（// 実行: jsc dev/prelude.js /tmp/staff.js dev/test_matchday_report_staff.js）: daysAgo(n) をテスト内定義（test_dash_staff の dOff と同型）。fixture: D.p 3名、D.cal=[{id:901,date:daysAgo(2),type:'match',title:'vs A大',opp:'A大',ko:'14:00',squad:[{pid:1,num:1},{pid:2,num:16}]},{id:902,date:daysAgo(9),type:'match',title:'vs B大',squad:[]},{id:903,date:daysAgo(-5),type:'match',title:'vs C大',squad:[]}]、D.md=[v2 pid1 evId901 minutes80 rpe9 sleepH5 hiaImpact:true hiaSymptoms:['頭痛'] , 旧md pid3 date=daysAgo(20) fatiguePre3 fatiguePost5 sleepTime/wakeTime evId無し]、D.i=[hia i approved:null pid1]。検証: (1) V.matchview に 3試合＋旧md日付の疑似行が出る／提出 1/2名／HIA 1／未提出 1／daysAgo(-5) は UPCOMING 節に『メンバー表』のみ／squad 空の 902 は『メンバー表未登録』／旧md 行は『（カレンダー未登録）』。(2) goMatchReport('901'): 未提出チップに選手2＋onclick goAddMatchDay('901',2)／要対応の先頭が pid1（HIA疑い タグ）／負荷 720 AU／睡眠不足 タグ／pitchProgressHtml 出力あり／.rv-armed を含まない。(3) goMatchReport(daysAgo(20)) の疑似イベント: 旧md 行に『前3/6→後5/6』・負荷 '-'。(4) goMatchDetail(v2 id): HIA ボックス・承認待ち バッジ・代理/提出メタ・escapeHtml（note に <b> を入れて &lt;b&gt; になる）。(5) goMatchDateDetail(date) が goMatchReport に委譲され例外なく描画。
- 【新規】dev/test_matchday_proxy_staff.js（staff）: setKey/store/drain は test_p7c と同型。fixture: cal 901（squad pid1 num1, pid2 num17）。(1) goAddMatchDay('901',2) → __timeouts 実行後 #smd-role='reserve'・#smd-num=17・#smd-min=''。(2) doAddMatchDay 検証順: 選手未選択→alert かつ btn.dataset.busy!=='1'（固着しない）／minutes 未入力→alert／minutes>0 で rpe 未入力→alert／sleepH 未入力→alert／pre 未入力→alert。(3) 正常保存（怪我なし）: store('md') に v:2/evId:901/proxy:true/recordedBy:'スタッフ'/minutes/rpe/sleepH、toast に『代理入力しました』、i/r は増えない。(4) 重複: 同 pid で再度 doAddMatchDay → md 増えず toast『入力済み』＋ showEditMatchStaff 呼出（スタブで検知）。(5) 怪我あり: store('i') に source:'match'/approved:true/proxy:true/mdId=md.id/canPractice、store('r') に injId、md.injId 一致。(6) HIA（衝撃あり＋症状1つ）: i に type:'脳震盪'/part:'頭部'/hia:true、md.hiaInjId、drain 2回後 store('chart') の該当 injId が isConcussion===true かつ injType==='脳震盪'。(7) injured かつ type 脳震盪 → 怪我 i は1件のみ（HIA 二重起票しない）で hia:true。(8) 途中失敗の再送: runTransaction を2件目で reject するモック→alert に『件保存済み』、_smdPending あり、モック復旧後に再度 doAddMatchDay → 残り items のみ追加され id 重複なし。(9) showEditMatchStaff(v2 id): __timeouts 後 #emds-min/#emds-rpe/#emds-sleeph が既存値、doEditMatchStaff で minutes 80→63 に訂正され editedAt/editedBy が付き inputAt/proxy 不変。旧md id: #emds-fpre 等の旧フォームが出て #emds-date で日付訂正できる。(10) delMatchDayStaff: __alerts に confirm 由来の停止が無く即削除、toast の actionLabel==='元に戻す'、fn 実行→store('md') に復元、injured なら msg に『怪我報告は残ります』。
- 【新規】dev/test_matchday_dash_staff.js（staff）: test_dash_staff.js の renderDash() を流用。fixture: cal に daysAgo(0)/daysAgo(2)/daysAgo(3)/daysAgo(4) の match（squad pid1,pid2）、md は pid1 の daysAgo(2) のみ。検証: (1) daysAgo(0)/2/3 のカードが出て daysAgo(4) は出ない（『試合日チェック』見出し数=3）。(2) daysAgo(2) は『提出 1/2』＋未入力チップ選手B（onclick goAddMatchDay）、daysAgo(0) は『提出 0/2』。(3) squad 空の試合は『メンバー表未登録』で chip-late 無し。(4) 旧『昨日の試合日チェック未入力』文言・D.matchsel 参照が無い（D.matchsel=undefined でも例外なし）。(5) HIA: D.i に {hia:true,source:'match',approved:null,date:daysAgo(2)} と通常 pending を入れ、新着怪我の先頭行に『【HIA】』、ボタン文言『（脳震盪として）』。(6) approveInjury(hiaId) → drain×2 → store('i').approved===true＋approvedBy／store('chart') isConcussion===true・injDetail.scene==='公式戦'。非HIA approveInjury は chart を作らない。(7) approved===false の報告に approveInjury → blocked alert・変更なし。(8) doDelPlayer(2): store('cal')[*].squad から pid2 が消え他 pid は残る（confirm はモックで true）。(9) 生hex/rgba が出力に含まれない（/#[0-9a-f]{3,6}\b|rgba?\(/i が false）。
- 【任意】dev/test_matchday_care_trainer.js（// 実行: jsc dev/prelude.js /tmp/trainer.js ...）: SK.md 存在、T.home に『試合後の要ケア』カード、HIA 行が先頭、approved:null は『承認待ち』でリンク無し、approved:true は goRehabPlayer リンク。
- 【既存・維持確認】dev/test_p7c.js 73-82（approveInjury の approvedBy/approvedByRole/approvedAt/notifyPlayer＝svSafeUpdate 化後も drain() 1回で成立）／dev/test_dash_staff.js（D.cal=[]・D.md=[] なので matchPanel は空・既存 ok 群不変。D.matchsel=[] の代入は残しても無害）／dev/test_mstat.js 113（ms 索引に kind 無し＝本フェーズは ms 非接触）／dev/test_staff_ia_p8d.js（試合/help 参照なし）／dev/test_dash.js 74-88・dev/test_home_p8b.js 31-38 は player 側＝P1b の責務（P1c は player 非接触）。
- 【手順】python3 dev/run_tests.py → python3 dev/sync_check.py → python3 dev/sync_check.py --residue が全て緑。単発: python3 dev/extract.py staff/index.html /tmp/staff.js && jsc dev/prelude.js /tmp/staff.js（構文チェック・cwd=リポジトリルート）。

## syncManifest
- identical 追加: svSafeSeq {files:[player,staff,trainer],kind:function}（player 1015 と trainer 850 は正規化後一致を確認済み。staff へ移植後に登録）
- identical 追加（P1b が未登録なら）: rate5HTML/setRate5 {files:[player,staff],kind:function}、_rate5OnChange/RATE5_LABELS/SORE_EMO/STRESS_EMO {files:[player,staff],kind:var}、partChipsHTML/togglePartChip/clearPartChips/bindSorePartsToggle {files:[player,staff],kind:function}、listChipsHTML/toggleListChip {files:[player,staff],kind:function}、MD_CRAMP_WHEN/MD_CRAMP_PARTS/MD_HIA_SYMPTOMS {files:[player,staff],kind:var}（名前は P1b の実名に合わせる）
- identical 追加（P1a 基盤の files に staff を含める・未登録なら登録）: matchEvents/matchEventByDate/squadOf/squadRole/mdOf/mdRoleLabel/mdLoad/pendingMatchChecks/mdInjuryLive/mdSleepStr/mdFatigueStr {files:[player,staff(,coach)],kind:function}
- identical 追加（trainer 前倒しステップ14を採る場合のみ）: injIsHia/mdIsHiaSuspect/mdsOfEvent/mdTriage/matchEventTitle {files:[staff,trainer],kind:function}、および P1a 基盤の files に trainer を追加。採らない場合は staff ローカル（単一ファイルは登録不要）で、P5 の coach 展開時に identical 昇格。
- variant: 変更なし（selectPSearch は staff 固有・未登録）。chart_counts: 変更なし（staff 12 のまま）。
- 登録後 python3 dev/sync_check.py 緑、python3 dev/sync_check.py --residue 違反 0 を確認。

## openIssues
- P1a/P1b の関数名・シグネチャ確定待ち: 本設計は matchEvents/matchEventByDate/squadOf/squadRole/mdOf/mdRoleLabel/mdLoad/mdInjuryLive/mdSleepStr/mdFatigueStr（P1a）と rate5HTML 群・listChipsHTML/toggleListChip・MD_* 定数・RATE5_LABELS.fatigue/perf（P1b）を仮名で参照している。実名が違えばステップ2の薄いラッパで吸収するか名前を揃える。
- squad エディタの関数名: 本設計は既存名 goSelectMatchMembers(evId,date) が P1a で squad 編集に改修済みと仮定。P1a が別名（例 goSquadEditor）にした場合は V.matchview/レポート/ダッシュボードの『メンバー表』『登録する』ボタン3箇所を差し替える。
- 代理入力の重複ガードは事前チェックのみ（svSafeSeq は append 専用で updateFn 内再判定ができない）。選手の自己提出とスタッフ代理入力が同時刻に競合すると md が2件になりうる→P2 で『同 pid×evId の重複を編集画面で警告し片方削除を誘導』の保険を検討。
- svSafeSeq の非原子性: 怪我あり代理入力で i/r 保存後に md が失敗すると _smdPending 再送まで i が孤児化（approved:true で怪我一覧に出る）。再送 UI は alert 文言のみ＝ボタン化（『未送信分を再送』）は P2 候補。
- HIA 承認時の chart 作成が失敗した場合の再適用経路: approveInjury は approved:true 後は再実行されないため、失敗 toast で『評価タブの「脳震盪として扱う」を ON』を案内する運用。自動リトライは未実装。
- evId 無しの旧 md は date 一致でイベントに紐づけるため、同日2試合（cal に同日 match が2件）では両方に集計される。P3 の ms/gs evId 紐づけと合わせて『同日複数試合は date 救済を先頭イベントのみ』にするか判断。
- mdTriage の『高負荷』閾値（rpe>=8 または同試合の負荷上位25%・提出8名以上のときのみ）と『睡眠<6h』『perf<=2』は暫定。凛人の監修で調整（コード上は mdTriage 1関数に閉じている）。
- doDelPlayer(5046)・resolveInj(5071)・msDelConfirm 等の confirm() は本フェーズ対象外のまま残る（CRUD雛形v2 違反の残置）。別フェーズで一括置換。
- reqQueue(1661)/updateQueueBadge の要対応件数に『試合日チェック未入力』を含めるか: 本設計は含めない（late と同様に恒常的に膨らみ警報機能を殺すため）。ダッシュボードのパネルのみで督促。
- trainer 前倒し（ステップ14）を P1c に含めるか P5 に回すか: 安全上は P1c 推奨（HIA 承認→isConcussion までは P1c で完結し、trainer は既存 trTodoBadge で安全ゲートが動くため、ステップ14 は『承認前の可視化』の付加価値のみ）。
- exportCSV('matchday') は最小互換のみ（undefined 混入回避）。試合単位出力・列拡張・"エスケープは P5。
- 行番号は現行 HEAD（bc735ac）基準。P1a/P1b の変更で staff/index.html の行がずれるため、実装時は関数名で再検索すること。

## estimate
staff/index.html: 追加 約650〜800行・削除 約60行（V.matchview 置換 / goMatchReport 新設 ~120行 / goAddMatchDay+doAddMatchDay ~180行 / showEdit/doEdit v2 ~120行 / ヘルパー ~90行 / ダッシュボード ~60行 / approveInjury・HIA ~40行 / 部品移植 ~120行）。trainer（任意）: 約60行＋SK/D 2箇所。dev: 新規テスト3本（各120〜200行）＋manifest 登録。作業単位は「1機能→構文チェック→模擬実行」で 8 チェックポイント（①部品移植＋sync ②ヘルパー＋V.matchview ③goMatchReport＋goMatchDetail ④代理入力フォーム ⑤保存＋HIA連鎖 ⑥編集/削除Undo ⑦ダッシュボード＋approveInjury ⑧help/cascade/CSV＋全テスト）。所要 1.5〜2 セッション。完了後に 9/5 の試合（cal 登録→メンバー表→レポートの未提出チップから代理入力）を遡って投入可能。