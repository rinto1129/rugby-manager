# 引き継ぎ書 (HANDOFF)

> このファイルは「会話をクリアしても作業を引き継ぐ」ための申し送り。
> SessionStartフックで新セッション開始時に自動で読み込まれる。
> **作業の区切りごとにClaudeが更新する。** 古い情報は上書きしてよい。

---

## 最終更新
- 日時: 2026-06-24
- 更新者: Claude

## いま何をしているか（現在地）
- 「会話クリア対策」の仕組みを構築した。
  - `HANDOFF.md`（このファイル）＝引き継ぎ書
  - SessionStartフック＝クリア後に自動でこのファイルを読み込む
- これにより、クリアしても次のセッションが作業内容を把握できる状態になった。

## 直近で完了したこと
- サブエージェントレビューで弱点を洗い出し → 4サイトを作り替え。
- コミット `62dbe27`「データ保全強化: JSONバックアップ・全書込トランザクション化・XSSエスケープ」として確定 → **origin/main にプッシュ済み**。
- 内容: staff 605行 / player 273行 / trainer 208行 / coach 4行 の変更。

## リポジトリの状態
- ブランチ: main（origin/main と一致）
- 作業ツリー: クリーン（未コミット変更なし）
- 未追跡: `.DS_Store`, `CLAUDE.md`, `HANDOFF.md`（必要に応じてコミット）

## 次の一手（候補）
- レビューの続き（まだ対応していない弱点があれば）に進む。
- どの弱点に取り組むかはユーザーと相談して決める。

## 運用ルール（このプロジェクト固有）
- データは「短いキー」で読む。保存は `svSafe` / `svSafeUpdate` を使う。
- 1機能ずつ → 構文チェック → 動作確認 → 次へ。まとめて変更しない。
- git push の前は必ずユーザーに確認。それ以外の局所的・可逆な作業は確認不要で進める。
- 詳細は `CLAUDE.md` を参照（最重要ガイド）。
